<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.fjyt.asset.management.mapper.AddressMapper">

    <select id="getProvince" parameterType="Province" resultType="Province">
        select * from province
        <where>
            <if test="id != null">
                and id = #{id}
            </if>
            <if test="name != null and name != ''">
                and name = #{name}
            </if>
            <if test="provinceId != null and provinceId != ''">
                and province_id = #{provinceId}
            </if>
        </where>
    </select>

    <select id="getCity" parameterType="City" resultType="City">
        select * from city
        <where>
            <if test="id != null">
                and id = #{id}
            </if>
            <if test="name != null and name != ''">
                and name = #{name}
            </if>
            <if test="cityId != null and cityId != ''">
                and city_id = #{cityId}
            </if>
            <if test="provinceId != null and provinceId != ''">
                and province_id = #{provinceId}
            </if>
        </where>
    </select>

    <select id="getCounty" parameterType="County" resultType="County">
        select * from County
        <where>
            <if test="id != null">
                and id = #{id}
            </if>
            <if test="name != null and name != ''">
                and name = #{name}
            </if>
            <if test="countyId != null and countyId != ''">
                and county_id = #{countyId}
            </if>
            <if test="cityId != null and cityId != ''">
                and city_id = #{cityId}
            </if>
        </where>
    </select>
</mapper>